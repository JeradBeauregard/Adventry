doctype html
html
  head
    title Adventry - User Profile
  body
    h1 Welcome, #{user.username}!
    p
      strong Email: 
      | #{user.email}
    p
      strong User ID:
      | #{user.id}
    p
      strong Joined:
      | #{user.created_at}

    hr

    h2 Journaling
    a(href="/journal/journals") Go to Your Journals

    hr

    a(href="/logout") Log Out
