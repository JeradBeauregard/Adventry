doctype html
html
  head
    title Onboarding
    style.
      body {
        font-family: sans-serif;
        max-width: 800px;
        margin: auto;
        padding: 2rem;
        background-color: #f9f9f9;
      }
      h1 {
        color: #333;
      }
      form {
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
      }
      textarea {
        width: 100%;
        padding: 0.5rem;
        font-size: 1rem;
        border-radius: 6px;
        border: 1px solid #ccc;
        resize: vertical;
      }
      label {
        font-weight: bold;
        margin-bottom: 0.3rem;
        display: block;
      }
      button {
        width: fit-content;
        padding: 0.75rem 1.5rem;
        font-size: 1rem;
        border: none;
        border-radius: 6px;
        background-color: #4caf50;
        color: white;
        cursor: pointer;
      }
      button:hover {
        background-color: #45a049;
      }
  body
    h1 Let's get to know you 💬

    form(action="/journal/onboarding" method="POST")
      each question, i in questions
        div
          label(for=`q${i}`) #{question}
          textarea(name=`q${i}` required rows="4" cols="60")

      button(type="submit") Start Journaling →
